import modalStyles from "../styles/modal.module.css";

const Modal = () => {
    return (
        <Container className={modalStyles.container}>
            <p>Modal</p>
        </Container>
    );
};

export default Modal;
